@model WebSite.EndPoint.Models.ErrorViewModel

@{
    Layout = null;
    ViewData["Title"] = "خطا";
}
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>خطا - @Model.StatusCode</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css?family=VT323" rel="stylesheet">
    <style>
        body {
            background: #0414a7;
            color: #e0e2f4;
            font-family: 'VT323', monospace;
            text-align: center;
            padding-top: 10%;
        }

        h1 {
            font-size: 2.75rem;
        }

        h2 {
            font-size: 2.25rem;
        }

        p {
            font-size: 20px;
        }

        .container {
            width: 90%;
            margin: auto;
            max-width: 640px;
        }

        .title {
            margin-bottom: 50px;
        }

        .bg {
            background: #aaaaaa;
            padding: 0 15px;
            display: inline-block;
            color: #0414a7;
        }





        a {
            background: #aaaaaa;
            color: #0414a7;
        }
    </style>
</head>
<body>
    <main class="container">
        <h1 class="title"><span class="bg">Error - @Model.StatusCode</span></h1>
        <p> خطایی رخ داده است، برای ادامه:</p>
        <p>
            * به صفحه اصلی بازگردید.<br />
            * یا به مدیر وب‌سایت تماس بگیرید
        </p>
        <p>
            <a href="/home/index">صفحه اصلی</a>
        </p>
    </main>
</body>
</html>
